# Bypass `npm run build` so Nixpacks cannot inject `prisma migrate deploy`
# into the npm script. Call the build steps directly instead.
# Migrations are handled at runtime via startCommand in railway.json.
[phases.build]
cmds = ["npx prisma generate", "npx next build"]
